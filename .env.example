# SerpApi (reverse image search via Google Lens engine)
SERPAPI_API_KEY=your_serpapi_key_here

# Logging
LOG_LEVEL=INFO

# Blockchain (leave blank for local eth-tester chain — no values needed)
# Only fill these in later if/when we add the optional public testnet stretch goal
# ETH_RPC_URL=
# ETH_PRIVATE_KEY=
# CONTRACT_ADDRESS=